// Test301

program test13
// ------| Global variables

// ------| End gloabal variables
{
        void main()
        // ------| Local variables
        int a;
        char ach;
        bool abool;

        int a_array[];
        char ach_array[];
        bool abool_array[];
        // ------| End local variables
        {
            // ------| Code begin

            //* Obavezno a treba da bude >= 2!! */
                        read(a);
            //* Obavezno a treba da bude >= 2!! */

            a_array = new int[a];
            ach_array = new char[a];
            abool_array = new bool[a];

            // read(a);
            read(ach);
            read(abool);

            read(a_array[0]);
            read(ach_array[0]);
            read(abool_array[0]);

            print(a);
            print(ach);
            print(abool);

            print(a_array[0]);
            print(ach_array[0]);
            print(abool_array[0]);

            print(a_array[1]);
            print(ach_array[1]);
            print(abool_array[1]);

            // Result: Zavisi od unosa. Ali poslednja tri print bi trebalo da budu 0 [default za char] false

            // ------| Code end
        }
}
