// Test301

program test10
// ------| Global variables


// ------| End gloabal variables
{
        void main()
        // ------| Local variables
        int a;
        int b;
        int c;
        int d;
        int e;
        int f[];
        int result;
        // ------| End local variables
        {
            // ------| Code begin

            read(a);
            read(b);
            read(c);
            read(d);
            f = new int[4];
            f[0] = 1;
            f[1] = 2;
            f[2] = 3;
            f[3] = 4;
            result = a > 30 ? 1 : 0;
            //print(result);

            result = 10;
            result = (a / f[1] > b / f[1] ? c + f[1] : d - f[2]) <= (a > 3 ? f[3] + f[2] : f[1] + f[0]) ? (true ? d * f[1] - a + f[0] : 0) : (f[3] > 0 ? 10 : 0);
            print(result);

            // Result: Za unos 4 77 23 60 izlaz je 10 provereno u javi

            // ------| Code end
        }
}


